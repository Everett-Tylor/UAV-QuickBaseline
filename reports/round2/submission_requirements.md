# 复赛材料清单

1. 预测结果_1300张.zip：提交预测结果入口，1300 张原尺寸灰度 PNG，格式已校验。
2. Docker复现材料.zip：完整训练/验证/推理代码、脚本、固定划分、初始化及最终权重、环境和操作文档。
3. 技术方案.pdf：设计动机、方法、例代码、对照实验、结果与限制。

Docker 构建包不是可直接 docker load 的镜像。当前机器无 Docker，未生成镜像 TAR；解压后在 GPU Docker 环境执行 build_and_export.ps1 或 build_and_export.sh，完成构建和自检后导出。

截图没有给出各附件的上传入口、命名和大小限制，故三个产物分别提供。不要将源码、PDF 或模型加入预测 ZIP。
